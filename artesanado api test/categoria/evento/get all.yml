info:
  name: get all
  type: http

http:
  method: GET
  url: http://localhost:8080/categoria-evento
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
