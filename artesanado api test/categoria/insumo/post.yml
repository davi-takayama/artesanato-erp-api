info:
  name: post
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/categoria-insumo
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
